# https://hydra.cc/docs/configure_hydra/intro/

# enable color logging
defaults:
  - override hydra_logging: colorlog
  - override job_logging: colorlog

job:
  name: ${paths.timestamp}
  # ? Only used to validate the config
  # ? Generates a blank dir, safe to ignore
  num: 1208937
# output directory: follows the top-level `output_dir` (default a dynamic
# timestamped path under logs/${task_name}/runs/, overridable on the command line).
run:
  dir: ${output_dir}
sweep:
  dir: ${paths.log_dir}/${task_name}/multiruns/${hydra.job.name}
  subdir: ${hydra.job.num}

job_logging:
  handlers:
    file:
      # Incorporates fix from https://github.com/facebookresearch/hydra/pull/2242
      filename: ${hydra.runtime.output_dir}/${task_name}.log
